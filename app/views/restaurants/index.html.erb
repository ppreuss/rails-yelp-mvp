<h1>Restaurants#index</h1>
<p>Find me in app/views/restaurants/index.html.erb</p>

<ul>
  <% @restaurants.each do |resto | %>
  <li><%= resto.name %></li>
  <%= link_to "Details", restaurant_path(resto), class: "btn btn-primary"   %>
  <% end %>
</ul>
